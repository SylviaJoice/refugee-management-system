<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body>
    <%- include('../partials/header') %>
    <div class="app-body">
      <%- include('../partials/sidebar') %>
      <main class="content p-4">
        <div class="d-flex justify-content-between align-items-center mb-3">
          <h1>Medical Records</h1>
          <a href="/medical/add" class="btn btn-primary">Add Medical Record</a>
        </div>
        <%- include('../partials/messages') %>
        <div class="table-responsive">
          <table class="table table-hover align-middle">
            <thead class="table-light">
              <tr>
                <th>Refugee</th>
                <th>Illness</th>
                <th>Treatment</th>
                <th>Doctor</th>
                <th>Visit Date</th>
                <th>Actions</th>
              </tr>
            </thead>
            <tbody>
              <% records.forEach((record) => { %>
                <tr>
                  <td><%= record.full_name %></td>
                  <td><%= record.illness %></td>
                  <td><%= record.treatment %></td>
                  <td><%= record.doctor_name %></td>
                  <td><%= record.visit_date.toISOString().slice(0, 10) %></td>
                  <td>
                    <a href="/medical/edit/<%= record.record_id %>" class="btn btn-sm btn-secondary">Edit</a>
                    <form action="/medical/delete/<%= record.record_id %>" method="post" class="d-inline">
                      <button type="submit" class="btn btn-sm btn-danger" onclick="return confirm('Delete this medical record?');">Delete</button>
                    </form>
                  </td>
                </tr>
              <% }); %>
            </tbody>
          </table>
        </div>
      </main>
    </div>
    <%- include('../partials/footer') %>
  </body>
</html>
